from setuptools import setup


install_requires = ['abcEconomics']

setup(name='abcFinance',
      version='0.1',
      description='',
      url='https://github.com/AB-CE/abcFinance',
      packages=['abcFinance'],
      zip_safe=True,
      install_requires=install_requires)
